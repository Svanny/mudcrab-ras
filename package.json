{
  "name": "mudcrab-ras",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "An open source, interactive 3D atlas of a mudcrab recirculating aquaculture system.",
  "license": "MIT",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build && node scripts/check-budget.mjs",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/*.test.mjs",
    "package:cad": "python3 scripts/package_cad.py"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "three": "0.186.1"
  },
  "devDependencies": {
    "vite": "8.3.1"
  }
}
